<%= link_to "Return to Itineraries", itineraries_path %>
<h2><%= "#{@user.name}" %></h2>

<% if @user.unconfirmed_email %>
  <h3><%= @user.unconfirmed_email %> - Make sure to confirm your new email!</h3>
<% else %>
  <h3><%= @user.email %></h3>
<% end %>


<h3>Password: ***</h3>


<%= link_to "Edit Contact Information", edit_user_path(@user) %> <br>
<%= link_to "Change Your Password", edit_user_registration_path(@user) %> <br>
<%= link_to "Delete Account", user_path(@user),
        method: :delete,
        confirm: "Are you sure you want to delete your account?" %>
